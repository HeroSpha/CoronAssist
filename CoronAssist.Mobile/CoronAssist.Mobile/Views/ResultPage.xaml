<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="CoronAssist.Mobile.Views.ResultPage">
    <Shell.TitleView>
        <StackLayout VerticalOptions="CenterAndExpand">
            <Label Text="Screen Results" FontSize="Large" FontFamily="{StaticResource Regular}" VerticalOptions="CenterAndExpand" />
        </StackLayout>
    </Shell.TitleView>
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="60" />
            </Grid.RowDefinitions>
            <Frame Margin="5" HasShadow="True" BackgroundColor="{Binding BackgroundColor}">
                <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
                    <Label Text="{Binding Result}" FontSize="Header"  TextColor="White" HorizontalTextAlignment="Center"/>

                    <Label Text="{Binding Message}" FontSize="Small" TextColor="White" />
                </StackLayout>
            </Frame>
            <Button Grid.Row="1" Text="Book Appointment">
                
            </Button>
        </Grid>
    </ContentPage.Content>
</ContentPage>